# Database Configuration
# PostgreSQL connection string
# Format: postgresql://username:password@host:port/database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/chess

# Server Configuration
PORT=4000

# CORS Configuration
# Frontend URL for CORS (update for production)
CORS_ORIGIN=http://localhost:3000

# Production Notes:
# - On Railway, DATABASE_URL and PORT are automatically provided
# - Set CORS_ORIGIN to your deployed web app URL (e.g., https://chess-web.railway.app)
# - Set NODE_ENV=production for production deployments
# - See apps/api/.env.production.example for production template
