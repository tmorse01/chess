# Production Environment Variables for API Service
# These should be set in Railway dashboard or via Railway CLI

# Database (automatically provided by Railway Postgres plugin)
DATABASE_URL=postgresql://user:password@host:port/database

# Server Configuration (automatically provided by Railway)
PORT=3000

# CORS Configuration (REQUIRED - set to your web app URL)
CORS_ORIGIN=https://your-web-app.railway.app

# Environment Mode (recommended)
NODE_ENV=production

# Notes:
# - DATABASE_URL and PORT are auto-injected by Railway
# - CORS_ORIGIN must match your deployed web app URL
# - Update CORS_ORIGIN after deploying web service
