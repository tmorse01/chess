# Production Environment Variables for Web Service
# These should be set in Railway dashboard BEFORE building

# API URL (REQUIRED - must be set before build)
VITE_API_URL=https://your-api-service.railway.app

# Important Notes:
# - VITE_* variables are compiled into the build at BUILD TIME
# - If you change VITE_API_URL, you must rebuild/redeploy the web service
# - Get the API URL from Railway dashboard after deploying the API service
# - Format: https://your-service-name.railway.app (no trailing slash)
